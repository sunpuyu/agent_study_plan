# 参考
这次没有参考，直接使用opencode，告诉我应该如何完成今天的任务，opencode给了我思路，给了我代码，照着打了一遍

# 练习
1、使用openai SDK做了个简单的脚本，调用模型测试问题

# 笔记
1、python知识回顾：list列表，tuple元组，set集合，dict字典
2、今天用到的python库：dotenv、pathlib、pydantic、openai
3、之前一直困扰我的问题：我是用vscode开发，我没有安装github copilot，但是一直可以自动补全代码，而且效果很好，今天才发现是TraeCode插件在工作，牛！
4、AI结构化输出：第一层：**prompt约束（最弱）**；第二层：**原生JSON模式（response_format={"type": "json_object"}）**；第三层：**JSON Schema（最强）：response_format={"type": "json_schema", "json_schema": {..., "strict": True}}**